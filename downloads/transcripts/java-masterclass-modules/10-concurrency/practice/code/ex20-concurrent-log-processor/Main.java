import java.util.List;
import java.util.Map;

public class Main {
    public static Map<String, Integer> process(List<String> lines) throws Exception {
        // TODO: regex + bounded/concurrent processing + error policy + shutdown
        return Map.of();
    }
}
