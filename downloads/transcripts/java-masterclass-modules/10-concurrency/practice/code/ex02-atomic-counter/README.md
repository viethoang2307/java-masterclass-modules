# Bài 02 — Atomic counter (Medium)

Cho `threads` worker, mỗi worker tăng counter `increments` lần. Trả về counter cuối. Không dùng `synchronized`; chọn atomic primitive phù hợp và không làm mất update.
