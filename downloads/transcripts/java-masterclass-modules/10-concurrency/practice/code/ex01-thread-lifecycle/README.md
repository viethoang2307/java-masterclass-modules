# Bài 01 — Thread lifecycle (Medium)

Tạo một worker thread có tên `worker-1`, chạy task ghi `worker-done`, `join` worker và trả về kết quả. Không được gọi `run()` trực tiếp. Hoàn thành khi `SelfCheck` in `PASS`.

**Gợi ý:** Một writer và `join` là đủ để publish kết quả; không cần sleep.
