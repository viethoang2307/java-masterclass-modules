public class Main {
    public static String outcome(boolean dbSuccess, boolean publishSuccess) {
        // TODO: biểu diễn atomic DB + outbox, không tuyên bố exactly-once giả
        return "";
    }
}
