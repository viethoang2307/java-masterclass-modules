# 421 — Fake, mock, stub và spy

Stub trả response cố định; fake implementation nhẹ chạy được; mock verify interaction; spy bọc object thật. Ưu tiên fake/state test khi behavior quan trọng, mock ở boundary side effect.

Mock quá chi tiết làm test vỡ khi refactor không đổi behavior.
