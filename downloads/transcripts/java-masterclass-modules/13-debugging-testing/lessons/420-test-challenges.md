# 420 — Test challenge workflow

Mỗi challenge nên có contract, fixture, happy path, boundary, error path và cleanup. Khi fail: đọc assertion message, giảm input, xác định first divergence rồi viết regression test.
