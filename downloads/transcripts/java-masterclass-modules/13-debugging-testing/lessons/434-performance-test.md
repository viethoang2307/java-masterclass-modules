# 434 — Performance test

Performance test cần workload, warm-up, environment, measurement method và threshold. Đo p50/p95/p99, throughput, allocation và error; không chỉ average.

Microbenchmark ngây thơ dễ bị JIT/dead-code/GC đánh lừa; benchmark production-like trước khi tối ưu.
