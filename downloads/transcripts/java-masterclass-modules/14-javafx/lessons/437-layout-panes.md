# 437 — Layout panes

`VBox/HBox` sắp xếp một chiều, `BorderPane` theo vùng, `GridPane` theo cell, `AnchorPane` theo anchor, `StackPane` chồng node. Ưu tiên layout responsive thay vì hard-code tọa độ.

Padding/gap/grow constraints là phần design contract; test model không phụ thuộc pixel.
