# 448 — Key events và toolbar

Key event có capture/bubble và focus owner. Shortcut nên map vào command dùng chung với toolbar/menu, không duplicate logic.

Phải xử lý accessibility: mnemonic, focus traversal và không chỉ dựa vào mouse.
