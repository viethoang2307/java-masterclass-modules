# Bài 13 — Migration report (Very Hard)

Với artifact kind `named`, `automatic`, `legacy`, sinh action: `verify-descriptor:<name>`, `pin-automatic-name:<name>` hoặc `keep-classpath:<name>`. Artifact không hợp lệ tạo `review:<name>`.

Giữ thứ tự input và không làm mất evidence `moduleName`; báo module name automatic nếu có.

