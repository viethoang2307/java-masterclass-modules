import java.util.Set;

public class Main {
    static Set<String> requires(String descriptor) {
        // TODO: parse module names after requires, including transitive/static forms.
        return Set.of();
    }
    static Set<String> exports(String descriptor) {
        // TODO: parse package names after exports.
        return Set.of();
    }
}
