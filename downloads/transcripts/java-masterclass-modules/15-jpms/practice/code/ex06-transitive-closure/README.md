# Bài 06 — Transitive closure (Hard)

Từ root module, tính toàn bộ module có thể đọc qua các cạnh `requires`. Kết quả phải chứa root, không lặp và chạy đúng khi graph có cycle hoặc module chưa có trong map.

Dùng kết quả để mô phỏng module set đầu vào cho `jlink`.

