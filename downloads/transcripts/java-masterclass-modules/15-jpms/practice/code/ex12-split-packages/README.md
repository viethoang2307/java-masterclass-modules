# Bài 12 — Split-package detection (Hard)

Input map module → package set. Trả về các package xuất hiện ở từ hai module trở lên. Bỏ qua module/package null và giữ kết quả không trùng.

Đây là bước inventory trước migration; không tự động gộp module hoặc đổi tên package.

