import java.util.List;

public class Main {
    static List<String> validate(String descriptor, String service, List<String> providers) {
        // TODO: check uses service and provides service with every provider.
        return List.of();
    }
}
