# Lời giải — Module-path command

Validate ba thành phần rồi quote module path, nhưng không quote module/class expression. Command dùng entry point module-mode `module/class`, không lẫn `-cp`. Độ phức tạp O(n) theo độ dài chuỗi.

