# Lời giải — Migration report

`switch` trên artifact kind giữ policy tập trung và dễ review. Action giữ nguyên thứ tự input để report có thể đối chiếu artifact inventory. Artifact chưa phân loại được đưa vào `review`, không âm thầm coi là legacy.

