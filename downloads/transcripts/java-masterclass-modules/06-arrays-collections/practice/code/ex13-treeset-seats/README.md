# Exercise 13 — TreeSet Seats

**Độ khó:** Very Hard. Tạo theatre chứa seat duy nhất theo `(row, number)`, sort theo row rồi number. `bookAtOrAfter(row,number)` lấy `ceiling` và xóa seat đó; nếu không có trả `null`. Price không tham gia identity/order.
