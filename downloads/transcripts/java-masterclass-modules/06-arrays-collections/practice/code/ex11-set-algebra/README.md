# Exercise 11 — Set Algebra

**Độ khó:** Hard. Trả `Algebra(union, intersection, leftOnly, symmetricDifference)` từ hai set. Không mutate input, reject null và trả các set immutable. Bao phủ `addAll`, `retainAll`, `removeAll`.
