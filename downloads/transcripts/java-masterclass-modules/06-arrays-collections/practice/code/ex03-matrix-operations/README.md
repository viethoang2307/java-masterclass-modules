# Exercise 03 — Matrix Transpose

**Độ khó:** Medium. Viết `transpose(int[][])` cho ma trận rectangular không rỗng. Reject `null`, row `null`, zero columns hoặc jagged input. Không mutate input. Self-check phải in `PASS`.
