# Exercise 09 — Enum Priority

**Độ khó:** Hard. Mỗi `Priority` có weight và SLA hours. Sort immutable copy của jobs theo weight giảm dần rồi name tăng dần; không mutate input. `label()` dùng switch expression.
