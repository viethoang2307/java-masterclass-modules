import java.util.Map;
public class Main {
    static Map<String, Integer> frequencies(String text) {
        // TODO: tokenize, normalize, merge counts, then return a sorted immutable map.
        return Map.of();
    }
}
