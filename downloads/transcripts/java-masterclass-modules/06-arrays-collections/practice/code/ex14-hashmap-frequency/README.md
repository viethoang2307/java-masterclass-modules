# Exercise 14 — Word Frequency

**Độ khó:** Very Hard. `frequencies(String)` tokenize Unicode letters/digits, lowercase bằng `Locale.ROOT`, đếm bằng `Map.merge` và trả immutable map sort theo key. Blank text trả empty map.
