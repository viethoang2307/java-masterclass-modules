# Exercise 15 — Compute Inventory

**Độ khó:** Very Hard. Xây `Inventory`: normalize SKU, `receive` quantity dương bằng `merge` và exact arithmetic; `consume` chỉ mutate nếu đủ stock, xóa entry khi về 0; `snapshot` immutable và sort key.
