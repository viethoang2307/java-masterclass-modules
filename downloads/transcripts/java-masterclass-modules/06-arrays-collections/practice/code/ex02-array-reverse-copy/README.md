# Exercise 02 — Reverse Copy

**Độ khó:** Medium. Hoàn thiện `reverseCopy(int[])`: trả array mới theo thứ tự đảo ngược, không mutate input; `null` gây `IllegalArgumentException`. Chạy bằng `javac --release 17 Main.java SelfCheck.java` và `java SelfCheck`.
