# Exercise 06 — LinkedList Itinerary

**Độ khó:** Hard. Hoàn thiện `insertOrdered(LinkedList<String>, String)`: trim destination, reject blank/null, chèn theo thứ tự không phân biệt hoa thường bằng `ListIterator`, không thêm duplicate. Trả `true` khi có thay đổi.
