# Exercise 08 — Boxing Ledger

**Độ khó:** Hard. Tính balance từ `List<Long>` bằng `Math.addExact`; reject list hoặc phần tử `null`, phát hiện overflow thay vì wrap. Viết method `append(List<Long>, long...)` để thêm transaction qua boxing.
