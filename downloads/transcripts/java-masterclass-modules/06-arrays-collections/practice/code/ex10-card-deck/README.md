# Exercise 10 — Card Deck

**Độ khó:** Hard. Tạo enum `Suit`, `Rank`, record `Card` và `standardDeck()` trả immutable list đủ 52 card duy nhất, theo suit rồi rank declaration order. Thêm `shuffle(deck, seed)` trả copy deterministic và không sửa deck.
