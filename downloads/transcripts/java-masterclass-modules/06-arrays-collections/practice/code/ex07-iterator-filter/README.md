# Exercise 07 — Iterator Filter

**Độ khó:** Hard. `removeInvalid(List<String>)` phải mutate list qua `Iterator.remove`, loại `null`, blank hoặc chuỗi không bắt đầu bằng letter/digit; trả số phần tử đã xóa. Không dùng `remove` trực tiếp trên list trong enhanced for.
