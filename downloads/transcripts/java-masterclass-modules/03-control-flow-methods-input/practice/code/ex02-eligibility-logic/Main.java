public class Main {
    static boolean canEnter(int age, boolean hasId, boolean vip) {
        // TODO: age must be valid; then use && and || deliberately.
        return false;
    }

    public static void main(String[] args) {
        System.out.println(canEnter(20, true, false));
    }
}

