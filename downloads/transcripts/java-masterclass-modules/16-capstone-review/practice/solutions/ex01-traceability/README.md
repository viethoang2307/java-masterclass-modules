# Lời giải — Requirements traceability

Duyệt một lần và đưa requirement vào report nếu thiếu domain, test, evidence hoặc ID. Output giữ thứ tự input để reviewer đối chiếu matrix. Độ phức tạp O(n), bộ nhớ O(r).

