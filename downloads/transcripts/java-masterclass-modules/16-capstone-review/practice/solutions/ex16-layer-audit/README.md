# Lời giải — Layer dependency audit

Allowed graph được khai báo như policy data, không rải switch trong loop. Edge unknown cũng bị báo. Sort kết quả làm CI/review stable. Độ phức tạp O(E log E) do sort.

