# Lời giải — Network status policy

Phân loại theo thứ tự success, transient, client, unknown; khoảng 500–599 được coi retryable. Policy O(1) và tách khỏi HTTP client để test không cần network thật.

