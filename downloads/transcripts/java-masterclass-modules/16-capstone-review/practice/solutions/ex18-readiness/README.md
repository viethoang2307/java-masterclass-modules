# Lời giải — Production readiness score

`score` chỉ cộng check pass nhưng validate weight của mọi row. Gate READY bảo thủ: không empty, không null, không fail và không negative weight. Độ phức tạp O(n).

