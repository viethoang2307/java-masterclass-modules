# Bài 07 — Error boundary (Hard)

Map exact kind `input`, `not-found`, `conflict`, `infrastructure` thành `INVALID_INPUT`, `NOT_FOUND`, `CONFLICT`, `UNAVAILABLE`; kind khác/null thành `INTERNAL_ERROR`.

Không trả exception message/SQL/token cho user-facing layer.

