# Bài 19 — Module/capability audit (Extreme)

Report `internal-export:<module>:<package>` khi export package kết thúc `.internal`; report `broad-opens:<module>` khi opens chứa `*`. Sort output để dùng trong CI.

Đây là policy review, không tự sửa descriptor.

