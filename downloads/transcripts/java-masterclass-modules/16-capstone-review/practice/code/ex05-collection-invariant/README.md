# Bài 05 — Collection invariant (Medium)

Danh sách ID hợp lệ khi list không null, từng ID không null/blank sau trim và không duplicate. Giữ phân biệt hoa thường (`a` khác `A`) vì ID là case-sensitive.

Bài này nhắc lại Set/Map và boundary validation trước khi bước vào streams.

