# Bài 06 — Stream report (Hard)

Đếm số track theo artist, bỏ qua track null và artist null/blank sau trim. Kết quả phải deterministic theo key (dùng map có thứ tự), không mutate input.

Test empty input, duplicate tracks và whitespace quanh artist.

