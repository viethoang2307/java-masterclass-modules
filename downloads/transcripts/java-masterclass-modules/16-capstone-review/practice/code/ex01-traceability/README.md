# Bài 01 — Requirements traceability (Medium)

Mỗi requirement phải có domain behavior, test và evidence (command/log/diagram). Trả về ID của các row thiếu ít nhất một cột; giữ thứ tự input, bỏ qua requirement null.

Đây là contract để review capstone không bị dừng ở “feature đã làm trên UI”.

