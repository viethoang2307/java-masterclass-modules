import java.util.List;
import java.util.Map;

public class Main {
    static List<Integer> plan(int current, int target, Map<Integer, Integer> migrations) {
        // TODO: follow one-step schema migrations, detect missing steps and cycles.
        return List.of();
    }
}
